[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=eduparse --cov-report=term-missing
markers =
    unit: Unit tests
    integration: Integration tests
    ocr: Tests for OCR functionality
    image: Tests for image processing
    claude: Tests for Claude AI integration 